import type { Metadata } from "next";
import { Poppins } from "next/font/google";
import "./globals.css";

const poppins = Poppins({
  variable: "--font-poppins",
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700"],
});

export const metadata: Metadata = {
  title: "The Fun Project - Bringing Joy, Building Community",
  description: "A 501(c)(3) pending nonprofit organization dedicated to bringing joy and building community through fun activities and events.",
  keywords: ["nonprofit", "community", "events", "volunteering", "joy", "fun"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${poppins.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
